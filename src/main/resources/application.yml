server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: ${SPRING_APP_NAME:bookwise}
  datasource:
    url: jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:bookwise_db}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  pattern:
    console: ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

twilio:
  account:
    sid: ${TWILIO_ACCOUNT_SID:ACcb4bed65b875e2349ca56c7c67c99d4e}
  auth:
    token: ${TWILIO_AUTH_TOKEN:93af76fe009c5d2385e655247e3a81b1}
  phone:
    number: ${TWILIO_PHONE_NUMBER:+12562696971}